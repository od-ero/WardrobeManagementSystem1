
'use client'

import Devices_form_body from '@/app/(app)/settings/(admin)/devices/devices_form_body'


const Mypage =() => {
    return <div>
        <Devices_form_body is_edit={ false } user={ '' } closeModal={``}/>
    </div>
}
export default Mypage;
